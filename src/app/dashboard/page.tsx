import Link from "next/link";
import { Upload, ShieldCheck, Layers3 } from "lucide-react";
const cards = [[Upload, "Upload images", "Upload files, paste a URL, or paste one image from your clipboard."], [Layers3, "Fallback chain", "Arrange Cloudinary, S3, ImageKit, and Firebase in the order you trust."], [ShieldCheck, "API keys", "Create scoped credentials for your automated uploads."]] as const;
export default function Dashboard() { return <main className="min-h-screen bg-slate-950 px-6 py-12 text-slate-100"><div className="mx-auto max-w-6xl"><p className="text-sm font-medium text-cyan-300">YOUR IMAGE INFRASTRUCTURE</p><h1 className="mt-2 text-4xl font-semibold">Every image has a way back.</h1><div className="mt-10 grid gap-5 md:grid-cols-3">{cards.map(([Icon, title, description]) => <Link className="rounded-2xl border border-slate-800 bg-slate-900 p-6 transition hover:border-cyan-400" href={title === "Upload images" ? "/dashboard/uploads" : title === "Fallback chain" ? "/dashboard/settings/providers" : "/dashboard/settings/api-keys"} key={title}><Icon className="text-cyan-300" /><h2 className="mt-5 text-lg font-medium">{title}</h2><p className="mt-2 leading-6 text-slate-400">{description}</p></Link>)}</div></div></main>; }
